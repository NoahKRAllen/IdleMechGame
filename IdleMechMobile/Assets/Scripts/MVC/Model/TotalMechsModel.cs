using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// army after it is created in factory
/// persistence for Mechs after built
/// </summary>
public class TotalMechsModel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
