using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Timer and save / load / run time differences
/// persistence for actual time clock when last exited so that we can calculate what happened while the player was gone
/// </summary>
public class TimeCycleModel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
