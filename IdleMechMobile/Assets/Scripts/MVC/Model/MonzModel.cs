using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Model for for Monz income and savings
/// persistence for savings and monz-per-cycle
/// probably will remove the monz-per-cycle as a data persist object and have it be a calculated field every time based on what planet you are looking at
/// </summary>
public class MonzModel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
